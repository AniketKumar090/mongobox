# MongoBox Voice Backend — Python dependencies

# ── Web server ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Voice cloning (Coqui XTTS-v2) ─────────────────────────────────────────────
TTS==0.22.0
transformers==4.41.2

# ── Deep learning runtime ─────────────────────────────────────────────────────
torch==2.3.0
torchaudio==2.3.0

# ── Audio utils (numpy pinned for TTS compatibility) ───────────────────────────
numpy==1.22.0
scipy>=1.9.0,<1.12

# ── Option B: extract instrumental from reference song ─────────────────────────
yt-dlp>=2024.1.0
demucs>=4.0.0
